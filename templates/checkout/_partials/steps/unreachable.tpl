{block name='step'}
  <section class="card checkout-step -unreachable" id="{$identifier}">
    <h1 class="card-header step-title h4">
      <span class="step-number">{$position}</span>. {$title}
    </h1>
  </section>
{/block}
